package bakery.classes;
///
// Container Interface which has the Iterator Interface  implementation the getIterator, which is common
// for both cake and cookie Name classes
///
public interface Container {
  public Iterator getIterator();
}
